# ==============================================
# 🔴 核心过滤规则（你的核心需求）
# ==============================================
# 过滤所有模块的 target 目录（编译产物，包括 jar、class 等）
**/target/
**/target/*
!**/target/classes/META-INF/  # 可选：保留 META-INF（如有需要，否则可删除这行）

# 过滤 Nacos 运行时目录（data=数据，logs=日志，均为临时文件）
shop-ease-infrastructure/nacos/data/
shop-ease-infrastructure/nacos/logs/
shop-ease-infrastructure/nacos/bin/derby.log  # Nacos 启动日志

# 过滤所有编译产物（jar、war、ear 等，双重保险）
*.jar
*.war
*.ear
*.class
*.dll
*.so
*.exe

# ==============================================
# 🟡 常用过滤规则（避免仓库污染）
# ==============================================
# 过滤 IDE 配置文件（IDEA、VSCode，团队协作不冲突）
.idea/
.vscode/
*.iml
*.iws
*.ipr
*.suo
*.log
.apt_generated/
.factorypath/
.springBeans/
.sts4-cache/

# 过滤系统临时文件（Windows/Mac）
.DS_Store  # Mac 系统
Thumbs.db  # Windows 缩略图
desktop.ini  # Windows 配置文件
*.tmp  # 所有临时文件
*.swp  # Vim 临时文件
*.swo  # Vim 临时文件

# 过滤日志文件（除业务日志外，运行时日志不提交）
*.log
*.logs/
logs/
log/

# 过滤数据库文件（如 MySQL 数据、SQL 备份，体积大且无用）
*.sql
*.db
*.sqlite
*.bak
*.dump

# 过滤压缩包（体积大，可通过其他方式传输）
*.zip
*.rar
*.7z
*.tar.gz
*.gz

# ==============================================
# 🟢 可选过滤（按需保留，不用可删除）
# ==============================================
# 过滤 Maven/Gradle 构建文件（本地构建用，无需提交）
*.maven.index
*.gradlew
*.gradlew.bat
gradle/
build/
.m2/
.npm/
.node_modules/

# 过滤证书/密钥文件（安全起见，绝对不提交）
*.pem
*.key
*.cert
*.pfx
*.jks