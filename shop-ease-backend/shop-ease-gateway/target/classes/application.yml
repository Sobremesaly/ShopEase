server:
  port: 8080
logging:
  level:
    # 网关核心转发日志（DEBUG级别，查看路由匹配、转发目标）
    org.springframework.cloud.gateway: DEBUG
    # 负载均衡日志（查看服务名解析为IP+端口的过程）
    org.springframework.cloud.loadbalancer: DEBUG
    # Nacos服务发现日志（确认网关获取到的服务实例）
    com.alibaba.cloud.nacos.discovery: DEBUG